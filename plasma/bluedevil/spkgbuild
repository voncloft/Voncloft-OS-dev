# description : Core components for the kde desktop

name=bluedevil
version=6.5.2
release=1

source="https://github.com/KDE/bluedevil/archive/refs/tags/v6.5.2.tar.gz"
options="!checksum"

build() {
   cd $name-$version
   plasma-package-installer
   cd build
   make DESTDIR=$PKG install
}
