# description	: Meta port for Xorg applications
# depends:	iceauth mkfontscale sessreg setxkbmap smproxy xauth xcmsdb xcursorgen xdpyinfo xdriinfo xev xgamma
# depends : xhost xinput xkbcomp xkbevd xkbutils xkill xlsatoms xlsclients xmessage xmodmap xpr xprop xrandr xrdb
# depends :	xrefresh xset xsetroot xvinfo xwd xwininfo xwd xwininfo xwud 
name=xorg-applications
version=1.0
release=1

build() {
	mkdir -pv $PKG/etc
}
